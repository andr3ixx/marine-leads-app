# Copy this to .streamlit/secrets.toml for local testing (gitignored).
# On Streamlit Community Cloud: App settings -> Secrets -> paste the same keys.

GROQ_API_KEY = "your_groq_key_here"
GOOGLE_PSI_API_KEY = ""  # optional — only needed if you enable the page-speed check
